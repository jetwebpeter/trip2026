services:
  - type: web
    name: travelpro
    runtime: node
    region: singapore
    plan: free
    buildCommand: cd frontend && npm install --legacy-peer-deps && npm run build && cd ../backend && npm install --legacy-peer-deps && npm rebuild better-sqlite3
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_VERSION
        value: 20
